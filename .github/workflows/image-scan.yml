
# uses Trivy image scanner for CVEs - Docker Image
name: Image Scanner

on: 
    workflow_call:

jobs:
    image-scanner:
        runs-on: self-hosted

        steps:
            - name: Checkout Code
              uses: actions/checkout@v4
              
            - name: Login to Docker Hub
              uses: docker/login-action@v3
              with:
                username: ${{ vars.DOCKER_USER }}
                password: ${{ secrets.DOCKER_PASS }}
            
              #By default trivy uses x64 architecture but my image is built on arm64 as runner is arm64
            - name: Trivy Scanner
              uses: aquasecurity/trivy-action@0.35.0
              with:
                image-ref: ${{ vars.DOCKER_USER }}/github-actions-app:${{ github.sha}}
                severity: 'CRITICAL,HIGH'
                exit-code: '1'
                trivyignore: ${{ github.workspace }}/.trivyignore
                platform: linux/arm64
